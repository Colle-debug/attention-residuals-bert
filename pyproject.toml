[project]
name = "attn-res-bert"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "datasets>=2.19",
    "numpy>=1.24",
    "tokenizers>=0.19",
    "torch>=2.1",
    "transformers>=4.40",
]
